version: "3.8"
services:
  api:
    build: .
    ports:
      - "8000:8000"
    # volumes:
    #   - "./models:/app/models"
    networks:
      - monitoring

  mlflow:
    image: ghcr.io/mlflow/mlflow
    ports:
      - "5001:5000"
    command: mlflow server --host 0.0.0.0
