apiVersion: v1
kind: Service
metadata:
  name: ecg-api-service
  namespace: ecg-mlops
spec:
  selector:
    app: ecg-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
