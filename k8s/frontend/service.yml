apiVersion: v1
kind: Service
metadata:
  name: ecg-frontend-service
  namespace: ecg-mlops
spec:
  selector:
    app: ecg-frontend
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
